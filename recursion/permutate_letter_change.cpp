#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    // It is same as permutate with space changes just that it is not compulsory that input has all lower cases
    // and also digits may be included

    // Now digits can be both lower and upper so it doesnt matter to digit so we will skip those ASCII values
    string input;
    string output;
    
    return 0;
}