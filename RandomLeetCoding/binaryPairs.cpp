#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    map<string, int> mp;

    int countBinarySubstrings(string s) {
        string temp = "";
        int zeroes = 0, ones = 0;
        for(int i=0; i<s.length(); i++){
            temp.push_back(s[i]);
        }
    }
};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    return 0;
}