#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    vector<string> vec;
    vector<string> readBinaryWatch(int turnedOn) {
        if(turnedOn > 8){
            return vec;
        }
        
    }
};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    return 0;
}